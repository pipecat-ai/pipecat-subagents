- ⚠️ Moved `LLMAgent` and `FlowsAgent` into their own subpackages. `LLMAgent` and `LLMAgentActivationArgs` now live in `pipecat_subagents.agents.llm`, the `@tool` decorator in `pipecat_subagents.agents.llm.tool_decorator`, and `FlowsAgent` in `pipecat_subagents.agents.flows`. Top-level imports from `pipecat_subagents.agents` (e.g. `LLMAgent`, `tool`) are unchanged; direct imports from `pipecat_subagents.agents.llm_agent`, `pipecat_subagents.agents.tool_decorator`, and `pipecat_subagents.agents.flows_agent` must be updated.
