- Modernized type annotations across all source files for Python 3.11+: `Optional[X]` replaced with `X | None`, `Callable`/`Coroutine` imported from `collections.abc`, and `isinstance` checks use tuple form instead of union operator.
