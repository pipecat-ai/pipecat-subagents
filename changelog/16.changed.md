- ⚠️ Removed the `parallel` parameter from the `@task` decorator. All task handlers now always run in their own asyncio task so the bus message loop is never blocked. Remove `parallel=True` or `parallel=False` from existing `@task` decorators.
